## Code Formatting Rules
- **Indentation**: Use **Tabs** exclusively for all code generation and refactoring.
- **Exception**: Use **Spaces (2 or 4 as appropriate)** only for languages where tabs are syntactically invalid or non-standard (e.g., YAML).
- **Consistency**: If editing an existing file, convert existing spaces to tabs unless it violates the language specification.
